# Task 070 - rule-engine-smoke

## Objective
规则层端到端 smoke。

## In scope
- 规则层端到端 smoke。
- 更新相关测试、文档或状态文件。

## Out of scope
- 不得静默扩大范围。
- 不得引入 wx 新依赖。

## Inputs
- 已完成的前置任务产物
- `AGENTS.md`、`plan.md`、`docs/coding_rules.md`

## Required changes
- 完成本任务目标对应的最小代码/文档修改。
- 更新必要测试或 smoke。
- 更新状态文件。

## Verification
- 说明本任务的最小可验证结果。
- 运行必要的 configure/build/test 或静态检查。
- 验证结果必须能支撑 `Done when`。

## Done when
- 本任务目标已实现。
- 对应验证已经执行且结果可说明完成状态。
- 状态文件已更新。

## Notes
- 本任务只处理当前边界，不得静默扩大范围。
- 完成后更新 `state/current_iteration.md`、`state/done.md` 或 `state/blocked.md`。
